# PM Board — только карточки проектов для проджекта (без сумм)
.DS_Store
node_modules
.next
deploy-standalone.tgz
*.db
*.db.backup*
.env*
!.env.example
deploy/team-tracker.env
*.tsbuildinfo
# Cursor: личные данные IDE игнорируем, но правила репозитория — в Git
.cursor/*
!.cursor/rules/
!.cursor/rules/*.mdc
